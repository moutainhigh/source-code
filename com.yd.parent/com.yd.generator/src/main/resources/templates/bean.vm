package ${package};

import java.io.Serializable;

import org.apache.commons.lang.builder.ToStringBuilder;
import org.apache.commons.lang.builder.ToStringStyle;
import java.util.Date;

/**
 *
 * This tools just a simple useful tools, gen table to javabean
 *
 * "I hope this tools can save your time"
 *
 * Generated by <tt>Generate</tt> 
 *
 * @author com.ypn.mapi.generate
 * @version v1.0
 */
public class ${className} implements Serializable{

	/** Comment for <code>serialVersionUID</code> */
	private static final long serialVersionUID = -1L;

	//========== properties ==========
	
#foreach($field in $fields) 
	private $field.simpleName $field.name;
#end

    //========== getters and setters ==========
    
#foreach($field in $fields) 
	public $field.simpleName ${field.getMethodName}() {
		return $field.name;
	}
	
	public void ${field.setMethodName}($field.simpleName $field.name) {
		this.$field.name = $field.name;
	}
#end

	/**
	* 重载toString方法
	* @return
	*
	* @see java.lang.Object#toString()
	*/
	@Override
    public String toString() {
		return ToStringBuilder.reflectionToString(this, ToStringStyle.SHORT_PREFIX_STYLE);
    }
}

