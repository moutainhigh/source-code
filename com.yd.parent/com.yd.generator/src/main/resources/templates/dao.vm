package ${package};

import java.util.List;
import java.lang.Integer;
import org.apache.ibatis.annotations.Param;
import ${beanPackage}.${className};
import ${reqPackage}.${className}Req;

/**
 *
 * This tools just a simple useful tools, gen table to javabean
 *
 * "I hope this tools can save your time"
 *
 * Generated by <tt>Generate</tt> 
 *
 * @author com.ypn.mapi.generate
 * @version v1.0
 */
public interface ${className}Dao {

	public ${className} find${className}ById(Integer id);
	
	public Integer insert(${className} param);
	
	public Integer get${className}Count(${className}Req req);
	
	public List<${className}> find${className}sByPage(@Param("req")${className}Req req,@Param("offset")Integer offset,@Param("rows")Integer rows);
	
	//if not use,pls delete it
	public Integer delete${className}ById(Integer id);
}